This program contains one python script, ValueIteration.py. It uses a Bellman equation to perform value iteration on a data grid and policy extraction to obtain the optimal route from the start to end point.

To run the program, first create a python virtual environment by typing "make". Then type python ValueIteration.py followed by the necessary command-line arguments.